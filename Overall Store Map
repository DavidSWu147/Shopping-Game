#Overall Store Map
import pygame
import time
pygame_init()
# setting up the window
display_width = 900
display_height = 800
white = (255,255,255)

store_display = pygame.display.set_mode(display_width, display_height)
pygame.display.set_caption("Welcome to the Store! Please click on an area to browse the avaliable products.")

pygame.display.update()

# defining checkpoint clicks, will be replaced with Evan's class definitions for checkpoints later
def store_area_click(x,y):
  pygame.getMouse()
    if pygame.getMouse = 
    else:
      break
