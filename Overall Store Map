#Overall Store Map
import pygame, math
from pygame import *
import time
pygame.init()
# setting up the window
display_width = 900
display_height = 800
white = (255,255,255)

pygame.display.set_mode((display_width, display_height))
pygame.display.set_caption("Welcome to the Store!")
pygame.setFill(white)
pygame.display.update()

# defining checkpoint clicks, will be replaced with Evan's class definitions for checkpoints later
def store_area_click(x,y):
  pygame.getMouse()
    if pygame.getMouse = 
    else:
      break
